// Resolver API configuration for production deployments.
//
// Set RESOLVER_URL to your deployed resolver API to skip the Connect UI.
// The dashboard will auto-connect on load and hide the URL input.
//
// For local development, leave this file as-is — the Connect bar will appear
// so you can point the dashboard at any running resolver (e.g. http://localhost:3000).
//
// On Vercel: either edit this file directly, or generate it in a build step:
//   echo "window.RESOLVER_URL = '$RESOLVER_URL';" > packages/dashboard/config.js
//
// window.RESOLVER_URL = 'https://your-resolver-api.example.com';
