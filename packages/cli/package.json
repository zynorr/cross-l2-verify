{
  "name": "@cross-l2-verify/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "cross-l2-verify": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "cli": "tsx src/index.ts"
  },
  "dependencies": {
    "@cross-l2-verify/sdk": "workspace:*",
    "commander": "^14.0.1",
    "ethers": "^6.15.0"
  }
}

