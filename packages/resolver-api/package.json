{
  "name": "@cross-l2-verify/resolver-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/server.ts",
    "test": "node --test --import tsx ./src/**/*.test.ts"
  },
  "dependencies": {
    "@cross-l2-verify/indexer": "workspace:*",
    "@cross-l2-verify/sdk": "workspace:*",
    "@types/express": "^5.0.3",
    "ethers": "^6.15.0",
    "express": "^5.1.0"
  }
}
